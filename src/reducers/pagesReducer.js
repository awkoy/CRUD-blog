import initialState from './initialState';

const pagesList = ( state = initialState.pages , action) => {
    switch (action.type)    {
        default:
            return state;
    }
};

export default pagesList;